Sync, guarenteed to be online

  Go through all local files
    If ID is local
      if authenticated
        Sync with new drive file
      else
        skip
    else // id is remote
      if file exists on drive
        sync it
      else
        if authenticated
          sync with new drive file
        else
          skip

    Go through all files marked deleted
      if file doesn't exist on drive
        delete it locally
      else


    get all remote files that we haven't processed already
      sync file locally


